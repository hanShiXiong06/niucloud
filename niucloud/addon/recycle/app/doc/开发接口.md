1. 特别声明
1、API接口同时支持 https和http 两种方式请求，推荐使用https方式，为了便于升级维护，建议采用服务后台对接云打印
2、API请求的参数包含2部分：公共参数和私有参数，详情请看对应API接口说明。
3、API请求和返回结果皆为 json 格式数据。
特别注意：开发者自己写的请求以表单方式提交数据，请求头需要使用Content-Type: application/json;charset=UTF-8，请勿使用其它方式提交数据

2. 第二步：对接平台API
1、 添加打印机到开发者账户（可批量） 【必接】
注意：打印之前必须调用本接口进行打印机注册，即添加PID或SN（查看打印机身底部标签）到开发者ID下，开发者也可以登录芯烨云开放平台，在【打印管理 ➜ 设备管理】下进行手动添加，在开放平台添加时，设备名称由开发者自己自由设置。
2、 设置打印机语音类型
3、 打印小票订单 【票据机必接】
4、 打印标签订单 【标签机必接】
5、 删除打印机（可批量）
6、 修改打印机信息
7、 清空待打印队列
8、 查询订单是否打印成功
9、 查询打印机某天的订单统计数
10、 查询打印机状态
11、 批量查询打印机状态
12、 金额播报

请求公共参数说明
所有请求都必须传递的参数。
参数名	是否必须	类型	说明
user	必须	string	芯烨云平台注册用户名（开发者 ID）
timestamp	必须	string	当前UNIX时间戳，10位，精确到秒
sign	必须	string	对参数 user + UserKEY + timestamp 拼接后（+号表示连接符）进行SHA1加密得到签名，值为40位小写字符串，其中 UserKEY 为用户开发者密钥
debug	可选	string	debug=1返回非json格式的数据，仅测试时候使用
sign参数说明：例如：user=acc、UserKEY=abc、timestamp=acbc，那么先拼成字符串accabcacbc，再将此字符串进行SHA1加密，得到sign。

返回公共参数说明
参数名	类型	说明
code	int	返回码，正确返回0，【注意：结果正确与否的判断请用此返回参数，添加打印机接口和删除打印机接口还需结合data一起判断】，错误返回非零。
msg	string	结果提示信息，正确返回”ok”，如果有错误，返回错误信息标识。
data	不指定	数据类型和内容详看私有返回参数data，如果有错误，返回null。
serverExecutedTime	int	服务器程序执行时间，单位：毫秒。



3. 回调地址设置说明：
（1）域名需经过ICP备案，新备案域名需24小时后才可配置。回调地址格式只支持英文大小写字母、数字及“-”，不支持IP地址。
（2）正式回调地址是线上正式运营的打印机设备用于接收芯烨云推送打印订单状态信息的地址，请您确保设置的正式回调地址端口为80或443，以免推送失败。
（3）芯烨云平台会对三方url进行存活监控，所有已配置的回调url，芯烨云会每分钟调用一次，请求参数按接口标准数据结构，参数内容为无效值（如：打印机编号 sn 字段为空、订单状态回调的订单号 orderNo 字段为空），商家返回 {"data":"OK"} 即可，以说明商家确认收到云平台的回调数据，请知悉。
（4）当您未填写或地址因平台检测为不可用时，平台将不再继续向该地址推送消息。
（5）填写好回调地址，点击【保存设置】校验通过，待存活监控检测有效后才生效。

打印订单状态回调URL地址
回调地址1：
未设置
回调地址2：
未设置
回调地址3：
未设置
回调地址4：
未设置
回调地址5：
未设置
批量回调：
 
批量订单状态回调请求参数为json数组格式，如：[{"orderNo" : "0" , "sn" : " " , "status" : 1}]
打印机状态推送回调URL地址
URL地址：
未设置
 
扫描枪和外接键盘回调地址
设置扫描枪内容回调URL：
未设置
 
设置键盘内容回调URL：
未设置
 
打印机暂停键自定义回调地址
打印机暂停键自定义回调地址：

