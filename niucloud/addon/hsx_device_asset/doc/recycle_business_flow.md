# 回收业务链路流程图

## 1. 回收主链路

```mermaid
flowchart TD
    A[用户提交回收订单] --> B{交付方式}

    B -->|快递寄送| C[用户填写寄件信息]
    B -->|到店/自送| D[用户预约或直接到店]

    C --> E[商家待签收]
    D --> E

    E --> F{是否签收}
    F -->|正常签收| G[订单已签收]
    F -->|拒收/异常| X1[订单异常处理]

    G --> H[设备进入待质检]
    H --> I[开始质检]
    I --> J[录入质检结果]

    J --> K{质检结论}
    K -->|可回收| L[进入定价]
    K -->|不回收/退回| R1[进入退回流程]

    L --> M[生成最终报价]
    M --> N[推送客户确认]

    N --> O{客户是否确认}
    O -->|确认报价| P[进入待打款]
    O -->|拒绝报价| R1
    O -->|超时未确认| N1[催办/再次通知]
    N1 --> N

    P --> Q[财务/工作人员打款]
    Q --> S{打款是否完成}
    S -->|完成| T[订单完成]
    S -->|部分打款| P1[保留待打款状态]
    S -->|失败/异常| P2[打款异常处理]

    R1 --> R2[确认退回方式]
    R2 --> R3[寄回/自取]
    R3 --> R4[设备已退回]
    R4 --> R5[订单关闭]

    T --> U[设备进入库存/后续销售链路]
```

## 2. 订单状态流转

```mermaid
stateDiagram-v2
    [*] --> 待签收

    待签收 --> 已签收: 商家签收
    待签收 --> 已取消: 用户/商家取消

    已签收 --> 质检中: 开始质检
    已签收 --> 已取消: 异常取消

    质检中 --> 已质检: 质检完成
    质检中 --> 待确认: 完成质检并定价
    质检中 --> 已取消: 异常取消

    已质检 --> 待确认: 生成报价
    已质检 --> 已取消: 异常取消

    待确认 --> 待打款: 客户确认报价
    待确认 --> 已关闭: 全部退回
    待确认 --> 已取消: 异常取消

    待打款 --> 已完成: 打款完成
    待打款 --> 已取消: 异常取消

    已完成 --> 已删除: 删除记录
    已关闭 --> 已删除: 删除记录
    已取消 --> 已删除: 删除记录
```

## 3. 设备状态流转

```mermaid
stateDiagram-v2
    [*] --> 待质检

    待质检 --> 质检中: 开始质检

    质检中 --> 已质检: 质检完成
    质检中 --> 待确认: 质检完成并生成报价
    质检中 --> 已退回: 质检不通过/客户退回

    已质检 --> 待确认: 生成报价
    已质检 --> 已退回: 退回客户

    待确认 --> 已回收: 客户确认
    待确认 --> 已退回: 客户拒绝/商家退回

    已定价 --> 已回收: 普通回收
    已定价 --> 已退回: 退回客户
    已定价 --> 已转代卖: 转入代卖

    已回收 --> [*]
    已退回 --> [*]
    已转代卖 --> [*]
```

## 4. 配置分支流程

```mermaid
flowchart TD
    A[回收订单进入系统] --> B{流转模式配置}

    B -->|整单流转| C[订单下所有设备共用主流程]
    B -->|按设备流转| D[每台设备独立质检/定价/处置]

    C --> E{交付方式配置}
    D --> E

    E -->|快递| F[记录快递公司/单号/物流状态]
    E -->|自送| G[记录到店/自送信息]

    F --> H{是否开启自动通知}
    G --> H

    H -->|开启| I[按节点发送通知]
    H -->|关闭| J[仅记录状态]

    I --> K{是否开启自动打印}
    J --> K

    K -->|开启| L[按触发场景生成打印任务]
    K -->|关闭| M[人工按需打印]

    L --> N[进入后续质检/定价/打款]
    M --> N
```

## 5. 质检与定价流程

```mermaid
flowchart TD
    A[设备待质检] --> B[质检人员领取/开始质检]
    B --> C[录入检测项]
    C --> D[上传质检图片/备注]
    D --> E{质检结果}

    E -->|通过| F[生成定价任务]
    E -->|需退回| R[进入退回流程]
    E -->|信息不足| B1[补充检测信息]
    B1 --> C

    F --> G[定价人员填写最终价格]
    G --> H{是否需要重新定价}

    H -->|否| I[推送客户确认]
    H -->|是| G1[重新定价]
    G1 --> I

    I --> J{客户确认结果}
    J -->|确认| K[待打款]
    J -->|拒绝| R
    J -->|未处理| L[催办/再次通知]
    L --> I
```

## 6. 打款与完成流程

```mermaid
flowchart TD
    A[客户确认报价] --> B[订单/设备进入待打款]
    B --> C{打款方式}

    C -->|整单打款| D[按订单确认打款]
    C -->|设备打款| E[按设备确认打款]

    D --> F{打款结果}
    E --> F

    F -->|全部完成| G[更新为已打款]
    F -->|部分完成| H[记录部分打款]
    F -->|失败| I[记录打款异常]

    H --> B
    I --> B

    G --> J{设备处置类型}
    J -->|普通回收| K[设备已回收]
    J -->|代卖| L[设备已转代卖]
    J -->|退回| M[设备已退回]

    K --> N[订单完成]
    L --> O[进入代卖链路]
    M --> P[订单关闭或部分完成]
```

## 7. 退回流程

```mermaid
flowchart TD
    A[触发退回] --> B{退回原因}

    B -->|客户拒绝报价| C[报价拒绝退回]
    B -->|质检不通过| D[质检退回]
    B -->|商家异常处理| E[人工退回]

    C --> F[确认退回设备]
    D --> F
    E --> F

    F --> G{退回方式}
    G -->|快递寄回| H[填写退回物流]
    G -->|客户自取| I[记录自取]

    H --> J[退回中]
    I --> J

    J --> K{客户是否收到}
    K -->|已收到| L[设备已退回]
    K -->|异常| M[退回异常处理]

    L --> N{订单内是否还有可回收设备}
    N -->|有| O[订单继续流转]
    N -->|无| P[订单关闭]
```

## 8. 代卖流程

```mermaid
flowchart TD
    A[设备已定价] --> B{处置类型}

    B -->|普通回收| C[进入库存]
    B -->|转代卖| D[创建设备代卖记录]
    B -->|退回| E[进入退回流程]

    D --> F[设置代卖价格/规则]
    F --> G[代卖中]

    G --> H{代卖结果}
    H -->|售出| I[记录成交价]
    H -->|取消代卖| J[返回库存/重新处置]
    H -->|需要调价| F

    I --> K[待结算]
    K --> L[代卖结算完成]
```

## 9. 通知触发流程

```mermaid
flowchart TD
    A[业务节点变化] --> B{通知配置是否开启}

    B -->|关闭| C[只记录业务日志]
    B -->|开启| D{通知场景}

    D -->|订单创建| E[发送下单通知]
    D -->|商家签收| F[发送签收通知]
    D -->|报价确认| G[发送确认通知]
    D -->|打款完成| H[发送打款通知]
    D -->|订单完成| I[发送完成通知]
    D -->|代卖状态变化| J[发送代卖通知]

    E --> K[写入通知日志]
    F --> K
    G --> K
    H --> K
    I --> K
    J --> K

    K --> L{发送结果}
    L -->|成功| M[通知完成]
    L -->|失败| N[记录失败原因/等待重试]
```

## 10. 打印触发流程

```mermaid
flowchart TD
    A[业务节点变化] --> B{打印配置}

    B -->|自动打印| C[匹配打印场景]
    B -->|手动打印| D[工作人员选择模板打印]
    B -->|不打印| E[流程继续]

    C --> F{场景是否匹配}
    F -->|匹配| G[生成打印任务]
    F -->|不匹配| E

    D --> G

    G --> H{打印机是否可用}
    H -->|可用| I[发送打印任务]
    H -->|不可用| J[打印任务失败/待重试]

    I --> K{打印结果}
    K -->|成功| L[记录打印成功]
    K -->|失败| J
```

## 11. 第三方服务边界

```mermaid
flowchart LR
    A[回收业务核心] --> B[通知服务]
    A --> C[打印服务]
    A --> D[物流/快递服务]
    A --> E[支付/打款记录]
    A --> F[后续销售/库存系统]
    A --> G[ERP/财务系统 预留]

    B --> B1[短信/公众号/小程序/企微]
    C --> C1[标签/质检单/退回单]
    D --> D1[寄件/签收/退回物流]
    E --> E1[打款凭证/支付状态]
    F --> F1[拍照/定价/上架]
    G --> G1[应收应付/成本/对账]
```

## 12. 回收完成后的出口

```mermaid
flowchart TD
    A[回收链路完成] --> B{设备最终去向}

    B -->|普通回收| C[设备入库]
    B -->|转代卖| D[进入代卖池]
    B -->|退回客户| E[退回完成]
    B -->|异常关闭| F[订单关闭]

    C --> G{是否进入销售链路}
    G -->|是| H[拍照/定价/上架系统]
    G -->|否| I[库存暂存]

    D --> J[代卖结算]
    E --> K[流程结束]
    F --> K
    H --> L[商城/ERP/销售系统]
```

