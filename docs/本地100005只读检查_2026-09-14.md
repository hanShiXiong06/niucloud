# 本地 100005 只读检查

用户要求“测试一下 100005”。本次按只读诊断处理，不把此请求解释为清空、重建或导入授权。

## 已验证结果

本地数据库连接成功，已确认连接为本机 `saas_`、表前缀 `ns_`。下列查询在数据库 `READ ONLY` 事务内执行，最后回滚并结束事务。

| 数据 | 100005 数量 |
| --- | ---: |
| 商城商品记录（含历史/删除状态，未筛选） | 30,355 |
| 商城 SKU 记录（未筛选库存与在售状态） | 20,643 |
| 商城订单 | 0 |
| ERP 设备 | 1 |
| ERP 采购单 | 1 |
| ERP 应付 | 1 |
| ERP 销售单 | 1 |
| ERP 应收 | 1 |
| ERP 结算记录 | 0 |
| ERP 仓库 | 1 |
| ERP 商城发布关联记录 | 0 |

不能把商品记录数当作可售设备数或可导入设备数。也不能仅凭这些数量断言应收、应付存在错误，或认定现有设备是否与商城正确关联。

## 规则测试

本轮重新执行 `tests/erp_mall_inventory_policy_test.php`，32 项断言通过，覆盖完整身份识别、六位尾号不作为完整 IMEI、重复/跨站关联阻断、缺失成本阻断及订单快照等纯代码规则。

这不是对 100005 现有业务的写入流程测试，不代表已经验证真实入库、付款、销售及退回全过程。

## 未完成检查及原因

只读检查脚本 `tools/maintenance/local_erp_mall_audit_100005.php` 已准备，PHP 语法检查通过。计划检查自营库存、完整串号、重复身份、成本、失效绑定，并调用实际库存对账预览及库存/应收/应付查询服务。

该完整检查尚未运行成功：工具授权审核服务两次返回 `Selected model is at capacity`，命令未获执行。没有改用其他入口绕过审核。

因此，可导入台数、逐项冲突原因、真实查询服务结果仍未知。本次没有清空、关联、导入、付款、通知或改变配置，也没有将原先针对 100000 的重建脚本改指向 100005。

## 后续边界

- 恢复工具授权后，可以继续同一只读脚本，不需要清库。
- 若要将重建目标由 100000 改为 100005，需要明确确认。该站点含大量存量商品，必须重新评估备份、身份和成本，不能照搬空商城站点的清理方案。
