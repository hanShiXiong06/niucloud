# 本项目修复工作约定

用户于2026-09-06明确：当前任务以问题修复为主。

- 优先修正业务根因，保证更新后的新操作不再出现同类问题，并做好明确提示和回归测试。
- 不默认扩展为历史数据回填、迁移、历史异常修复或旧版本兼容工程；用户另行提出时再处理。
- 不因“不做历史兼容”而删除既有业务记录、破坏正常功能或覆盖其他未完成的改动。
- 尽量复用已有表和字段；确实需要新增表、字段或数据库升级时，必须明确告知用户。

## 插件与框架边界

用户于2026-09-07明确：前端、后端都应尽量不侵入框架。

- 插件业务规则、页面、组件和配置优先放在插件目录，优先复用框架已有的参数、插槽、事件及服务。
- 公共框架中不硬编码插件名称、业务路由或某个插件的专用条件。
- 现有扩展点不足时，先说明原因及影响范围；只保留必要、通用且可选的扩展点，并将业务实现在插件内。
- 不为避免改框架而复制整套框架实现、隐式覆盖路由或绕过支付等关键校验。
- 交付时单独列出仍需更新的框架文件及原因，不将存在框架改动的方案称为“零侵入”。
